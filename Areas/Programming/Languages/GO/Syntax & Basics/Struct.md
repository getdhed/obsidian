# 🧱 Структуры (struct) в Go

> Struct — это составной тип данных, объединяющий несколько полей в одну логическую сущность.  
> В Go структуры — это фундамент архитектуры.

---

## 📌 Связанные темы

- [[Типы данных в Go]]
    
- [[Pointers]]
    
- [[Методы в Go]]
    
- [[Интерфейсы в Go]]
    
- [[Композиция вместо наследования]]
    
- [[Value vs Pointer в Go]]
    
- [[Память в Go]]
    

---

## 1️⃣ Объявление структуры

type User struct {  
	Name string  
	Age  int  
}

`User` — это новый тип данных.

Структура объединяет несколько полей под одним именем.

---

## 2️⃣ Создание структуры

### Инициализация через литерал (рекомендуется)

u := User{  
	Name: "Alex",  
	Age:  25,  
}

### Позиционная инициализация (не рекомендуется)

u := User{"Alex", 25}

⚠️ Если поменяется порядок полей — код сломается.

---

### Пустая структура

var u User

Поля получают нулевые значения:

- string → `""`
    
- int → `0`
    
- bool → `false`
    
- pointer → `nil`
    

Go **никогда** не оставляет мусор в памяти.

---

## 3️⃣ Передача структуры в функцию

По умолчанию структура передаётся **по значению**.

func changeName(u User) {  
	u.Name = "Bob"  
}

Это изменит копию.

---

### Передача по указателю

func changeName(u *User) {  
	u.Name = "Bob"  
}

Теперь изменяется оригинал.

Подробнее: [[Value vs Pointer в Go]].

---

## 4️⃣ Методы структур

Go не имеет классов, но поддерживает методы.

func (u User) Greet() string {  
	return "Hello, my name is " + u.Name  
}

---

### Метод с указателем

func (u *User) Birthday() {  
	u.Age++  
}

Если метод меняет состояние — используется указатель.

Связано с: [[Методы в Go]].

---

## 5️⃣ Вложенные структуры

type Address struct {  
	City string  
}  
  
type User struct {  
	Name    string  
	Address Address  
}

Доступ:

u.Address.City

---

## 6️⃣ Анонимные структуры

u := struct {  
	Name string  
	Age  int  
}{  
	Name: "Alex",  
	Age:  25,  
}

Используются для:

- временных данных
    
- тестов
    
- JSON-ответов
    

---

## 7️⃣ Встраивание (Embedding)

Go использует композицию вместо наследования.

type Person struct {  
	Name string  
}  
  
type User struct {  
	Person  
	Age int  
}

Теперь можно писать:

u.Name

Это называется **promotion полей**.

Связано с: [[Композиция вместо наследования]].

---

## 8️⃣ Сравнение структур

Структуры можно сравнивать, если все поля сравнимы:

u1 := User{"Alex", 25}  
u2 := User{"Alex", 25}  
  
fmt.Println(u1 == u2) // true

Если внутри есть:

- map
    
- slice
    
- func
    

— сравнение запрещено.

---

## 9️⃣ Struct Tags

type User struct {  
	Name string `json:"name"`  
	Age  int    `json:"age"`  
}

Теги используются для:

- JSON
    
- ORM
    
- валидации
    
- сериализации
    

Связано с: [[Сериализация в Go]].

---

## 🔟 Экспорт полей

type User struct {  
	Name string // экспортируемое  
	age  int    // приватное  
}

Если поле начинается с большой буквы — оно доступно вне пакета.

Это важно для:

- JSON
    
- работы между пакетами
    

Связано с: [[Модули и пакеты в Go]].

---

## 1️⃣1️⃣ Когда использовать указатель?

Используй `*struct`, если:

- структура большая
    
- нужно менять состояние
    
- метод модифицирует данные
    
- важно избежать копирования
    

Если структура маленькая — можно передавать по значению.

Подробнее: [[Value vs Pointer в Go]].

---

## 1️⃣2️⃣ Struct как архитектурная основа

В Go структуры используются для:

- моделей данных
    
- DTO
    
- конфигураций
    
- сервисов
    
- репозиториев
    
- состояния приложения
    

Пример:

type UserService struct {  
	repo UserRepository  
}

Go строится на композиции структур + интерфейсы.

Связано с:

- [[Clean Architecture]]
    
- [[Dependency Injection в Go]]
    
- [[Интерфейсы в Go]]
    

---

## 🧠 Философия

В Go нет:

- классов
    
- наследования
    
- конструкторов
    

Но есть:

- структуры
    
- методы
    
- композиция
    
- интерфейсы
    

И этого достаточно для построения сложных систем.

---

# 🔚 Вывод

Struct в Go — это:

- способ объединения данных
    
- основа композиции
    
- замена классов
    
- фундамент архитектуры языка
    

Если ты глубоко понимаешь struct — ты понимаешь половину Go.
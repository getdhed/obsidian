# 🧵 Context в Go

## Что такое Context

`context.Context` — это механизм управления жизненным циклом операций.

Он используется для:

- отмены операций
    
- установки таймаута
    
- дедлайна
    
- передачи request-scoped данных
    

Главная идея:

> Передавать сигнал "остановись" вниз по цепочке вызовов.

---

# 🧠 Почему context вообще нужен

В Go много:

- [[Areas/Programming/Languages/GO/Patterns & Recipes/Конкурентность/Goroutine]]
    
- сетевых запросов
    
- операций с базой данных
    
- HTTP-запросов
    

Если клиент отменил запрос — сервер должен:

- остановить обработку
    
- освободить ресурсы
    
- завершить горутины
    

Без context это сложно контролировать.

---

# 🧱 Интерфейс Context

type Context interface {  
    Deadline() (deadline time.Time, ok bool)  
    Done() <-chan struct{}  
    Err() error  
    Value(key any) any  
}

Самое важное:

Done() <-chan struct{}

Это канал, который закрывается при отмене.

---

# 🚦 Базовые контексты

## context.Background()

Корневой пустой контекст.

ctx := context.Background()

---

## context.TODO()

Используется временно, если ты не знаешь, какой контекст передать.

---

# ❌ Отмена через WithCancel

ctx, cancel := context.WithCancel(context.Background())  
  
go func() {  
    time.Sleep(2 * time.Second)  
    cancel()  
}()  
  
<-ctx.Done()  
fmt.Println("Контекст отменён")

### Что происходит:

1. Создаётся дочерний контекст
    
2. cancel() закрывает канал Done
    
3. Все горутины, слушающие Done, пробуждаются
    

---

# ⏳ Таймаут через WithTimeout

ctx, cancel := context.WithTimeout(context.Background(), 3*time.Second)  
defer cancel()  
  
select {  
case <-time.After(5 * time.Second):  
    fmt.Println("Завершилось")  
case <-ctx.Done():  
    fmt.Println("Таймаут:", ctx.Err())  
}

Вывод:

Таймаут: context deadline exceeded

---

# 📅 WithDeadline

То же самое, но с конкретным временем:

deadline := time.Now().Add(5 * time.Second)  
ctx, cancel := context.WithDeadline(context.Background(), deadline)  
defer cancel()

---

# 🌐 Context в HTTP

Каждый HTTP-запрос уже содержит контекст.

func handler(w http.ResponseWriter, r *http.Request) {  
    ctx := r.Context()  
  
    select {  
    case <-time.After(10 * time.Second):  
        fmt.Println("Обработка завершена")  
    case <-ctx.Done():  
        fmt.Println("Клиент отменил запрос")  
        return  
    }  
}

Если клиент закрыл соединение:

→ контекст отменится автоматически.

---

# 🧵 Context + Goroutine

Правильный способ:

func worker(ctx context.Context) {  
    for {  
        select {  
        case <-ctx.Done():  
            fmt.Println("Остановка worker")  
            return  
        default:  
            fmt.Println("Работаю...")  
            time.Sleep(1 * time.Second)  
        }  
    }  
}

---

# 📦 Передача данных через Context (WithValue)

type key string  
  
ctx := context.WithValue(context.Background(), key("userID"), 42)  
  
value := ctx.Value(key("userID"))  
fmt.Println(value)

⚠ Использовать только для request-scoped данных, а не для передачи зависимостей.

---

# 🔥 Иерархия контекстов

Background  
   └── WithCancel  
         └── WithTimeout

Отмена родителя отменяет всех детей.

---

# 🧠 Что происходит под капотом

Когда вызывается cancel():

- внутреннее поле `closed = true`
    
- канал Done закрывается
    
- все горутины, читающие `<-ctx.Done()`, пробуждаются
    

Закрытие канала будит всех.

---

# ⚠ Частые ошибки

1️⃣ Не вызывать cancel()

ctx, cancel := context.WithTimeout(...)  
defer cancel()

Всегда вызывать.

---

2️⃣ Хранить context в структуре

Плохо:

type Service struct {  
    ctx context.Context  
}

Context должен передаваться в функции, а не храниться.

---

3️⃣ Использовать context для передачи бизнес-данных

Context — не контейнер зависимостей.

---

# 🔗 Связанные темы

- [[Goroutine]]
    
- [[Select]]
    
- [[Channels]]
    
- [[HTTP Handlers]]
    
- [[gRPC]]
    
- [[Concurrency]]
    

---

# 📌 Главное понимание

Context — это:

> Механизм координации отмены и времени жизни операций в конкурентной системе.

Он не про данные.  
Он про управление временем и ресурсами.

---

# 💎 Инсайт для тебя

Когда ты пишешь:

client.GetUser(ctx, req)

Контекст:

- контролирует таймаут
    
- отменяет сетевой вызов
    
- останавливает работу в глубине стека
    

Это основа production backend.

# 🍪 Cookies (HTTP Cookies)

Cookie — это **маленькие данные (ключ-значение)**, которые сервер просит браузер сохранить и автоматически отправлять обратно при каждом [[HTTP]] запросе к этому же домену.

Проще:

> Cookie — это способ хранить данные на стороне клиента.

Связано с:

- [[HTTP]]
    
- [[HTTPS]]
    
- [[JWT]]
    
- [[Middleware]]
    
- [[API]]
    

---

# 🧠 Зачем нужны cookies

Чаще всего используются для:

- авторизации (session_id)
    
- хранения темы/языка
    
- запоминания пользователя
    
- корзины в интернет-магазине
    

---

# 🔄 Как это работает

### 1️⃣ Сервер отправляет cookie

Set-Cookie: session_id=abc123

### 2️⃣ Браузер сохраняет её

### 3️⃣ При каждом запросе браузер автоматически отправляет:

Cookie: session_id=abc123

---

# 📦 Структура cookie

Пример:

Set-Cookie: session_id=abc123; Path=/; HttpOnly; Secure; SameSite=Strict

Разберём параметры:

- **Path=/** → для каких путей отправлять
    
- **HttpOnly** → нельзя читать через JS (защита от XSS)
    
- **Secure** → только через [[HTTPS]]
    
- **SameSite** → защита от CSRF
    

---

# ⚠️ Важно понимать

Cookie — это просто хранение.  
Она **не равна** [[JWT]] и **не равна** серверной сессии.

Очень часто:

- Cookie хранит `session_id`
    
- Сервер хранит session в памяти/Redis
    

---

# 🚀 Работа с cookies в Go

Пакет используется стандартный:

net/http

---

## 🔹 Установить cookie

func SetCookieHandler(w http.ResponseWriter, r *http.Request) {  
	cookie := &http.Cookie{  
		Name:     "username",  
		Value:    "nazar",  
		Path:     "/",  
		HttpOnly: true,  
		Secure:   false, // true если HTTPS  
	}  
  
	http.SetCookie(w, cookie)  
	w.Write([]byte("cookie set"))  
}

---

## 🔹 Прочитать cookie

func GetCookieHandler(w http.ResponseWriter, r *http.Request) {  
	cookie, err := r.Cookie("username")  
	if err != nil {  
		http.Error(w, "no cookie", http.StatusBadRequest)  
		return  
	}  
  
	w.Write([]byte("Hello " + cookie.Value))  
}

---

## 🔹 Удалить cookie

Удаление — это установка с истёкшим временем:

func DeleteCookieHandler(w http.ResponseWriter, r *http.Request) {  
	cookie := &http.Cookie{  
		Name:   "username",  
		Value:  "",  
		Path:   "/",  
		MaxAge: -1,  
	}  
  
	http.SetCookie(w, cookie)  
	w.Write([]byte("cookie deleted"))  
}

---

# 🧩 Использование с Router

Можно использовать с:

- [[Router]]
    
- [[Chi Router]]
    
- [[HTTP Handlers]]
    
- [[Middleware]]
    

---

# 🔐 Безопасность cookies

### HttpOnly

Защищает от XSS.

### Secure

Работает только через [[HTTPS]].

### SameSite

Защищает от CSRF:

- `Strict`
    
- `Lax`
    
- `None`
    

---

# 📊 Cookies vs JWT

||Cookie|JWT|
|---|---|---|
|Где хранится|Браузер|Браузер/LocalStorage|
|Подписан|Нет|Да|
|Используется для|Session|Авторизация|
|Stateless|Нет|Да|

---

# 🧠 Частые ошибки

❌ Хранить пароли в cookie  
❌ Не ставить HttpOnly  
❌ Использовать без HTTPS  
❌ Класть чувствительные данные

---

# 📌 Когда использовать

Используй cookie если:

- делаешь классическую session авторизацию
    
- хочешь автоматическую отправку браузером
    
- работаешь с веб-приложением
    

Не всегда подходит для:

- мобильных API
    
- микросервисов
    

---

# 🔗 Связанные темы

- [[HTTP]]
    
- [[HTTPS]]
    
- [[JWT]]
    
- [[Middleware]]
    
- [[API]]
    
- [[REST API]]
    
- [[Status Code]]
# 🌐 REST API

## Что такое REST

REST (Representational State Transfer) — это архитектурный стиль построения веб-сервисов поверх [[HTTP]].

Главная идея:

> Ресурсы управляются через стандартные HTTP-методы.

REST — это **не протокол**, а набор принципов.

---

# 🧱 Основные принципы REST

## 1️⃣ Ресурс

В REST всё — это ресурс.

Примеры:

- Пользователь
    
- Книга
    
- Заказ
    

Ресурс имеет URI:

GET /users/15  
GET /books/10

---

## 2️⃣ Использование HTTP-методов

REST использует стандартные методы [[HTTP]]:

- GET — получить ресурс
    
- POST — создать ресурс
    
- PUT — обновить полностью
    
- PATCH — обновить частично
    
- DELETE — удалить ресурс
    

Пример:

POST /users  
DELETE /users/15

---

## 3️⃣ Stateless

REST-сервер **не хранит состояние клиента**.

Каждый запрос должен содержать всю необходимую информацию.

Пример:

- Токен передаётся в каждом запросе
    
- Сервер не "помнит", что было до этого
    

Связано с:

- [[HTTP]]
    
- [[JWT]]
    
- [[Authentication]]
    

---

## 4️⃣ Представление ресурса

Клиент получает представление ресурса.

Обычно в формате:

- [[JSON]]
    
- XML
    

Пример ответа:

{  
  "id": 15,  
  "name": "Nazar",  
  "email": "test@mail.com"  
}

---

# 🧠 REST ≠ HTTP

HTTP — транспорт.

REST — правила использования HTTP.

Можно написать HTTP-сервер, который не является REST.

---

# 📡 Как работает REST API

1. Клиент отправляет HTTP-запрос
    
2. Сервер обрабатывает
    
3. Возвращает HTTP-ответ с [[Status Code]]
    

Пример:

GET /users/15 HTTP/1.1

Ответ:

200 OK  
Content-Type: application/json

---

# 🔢 Статус-коды

REST активно использует [[Status Code]]:

- 200 — успех
    
- 201 — создано
    
- 400 — ошибка клиента
    
- 401 — не авторизован
    
- 404 — не найдено
    
- 500 — ошибка сервера
    

---

# 🔄 Idempotency

Некоторые методы являются идемпотентными.

[[Idempotency]] — повторный запрос даёт тот же результат.

- GET — да
    
- PUT — да
    
- DELETE — да
    
- POST — нет
    

Это важно для устойчивости системы.

---

# 🏗 REST в Go

Простейший пример:

http.HandleFunc("/users", handler)  
http.ListenAndServe(":8080", nil)

Обычно используется:

- [[Router]] (chi, gorilla, echo)
    
- [[Middleware]]
    
- [[Context]]
    

---

# 🔐 REST и авторизация

Часто используется:

- [[JWT]]
    
- Bearer Token
    
- OAuth
    

Пример:

Authorization: Bearer <token>

---

# 🧩 REST и архитектура

REST API обычно связан с:

- [[Controller]]
    
- [[Service Layer]]
    
- [[Repository Pattern]]
    
- [[Clean Architecture]]
    

---

# 📌 Когда использовать REST

Подходит для:

- CRUD-приложений
    
- Веб-сервисов
    
- Микросервисов
    
- Публичных API
    

---

# ⚠️ Ограничения REST

- Нет строгой типизации (в отличие от [[gRPC]])
    
- Может быть избыточен
    
- Много лишних данных (overfetching)
    

---

# 🔗 Связанные темы

- [[HTTP]]
    
- [[JSON]]
    
- [[TCP]]
    
- [[Socket]]
    
- [[Middleware]]
    
- [[JWT]]
    
- [[Idempotency]]
    
- [[Status Code]]
    
- [[gRPC]]
    

---

# 💎 Главное понимание

REST — это:

> Стиль построения API, где ресурсы управляются через HTTP-методы без хранения состояния на сервере.
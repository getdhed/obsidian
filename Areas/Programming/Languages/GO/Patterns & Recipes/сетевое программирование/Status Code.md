# 🔢 HTTP Status Codes

## Что такое статус-код

HTTP статус-код — это трёхзначное число, которое сервер возвращает в ответе, чтобы сообщить результат обработки запроса.

Пример:

HTTP/1.1 200 OK

Статус-коды работают на уровне [[HTTP]] (прикладной уровень [[OSI Model]]).

---

# 🧠 Зачем нужны статус-коды

Они позволяют клиенту понять:

- запрос успешен?
    
- ошибка клиента?
    
- ошибка сервера?
    
- нужно ли повторить?
    

Без них REST API был бы хаосом.

---

# 📚 Классы статус-кодов

Первая цифра определяет класс:

|Класс|Значение|
|---|---|
|1xx|Информационные|
|2xx|Успех|
|3xx|Перенаправление|
|4xx|Ошибка клиента|
|5xx|Ошибка сервера|

---

# ✅ 2xx — Успех

## 200 OK

Самый частый.

Используется для:

- успешного GET
    
- успешного PUT
    
- успешного PATCH
    

w.WriteHeader(http.StatusOK)

---

## 201 Created

Используется при создании ресурса.

w.WriteHeader(http.StatusCreated)

Обычно возвращают:

- созданный объект
    
- Location header
    

---

## 204 No Content

Успех, но без тела ответа.

Используется:

- после DELETE
    
- когда нечего возвращать
    

w.WriteHeader(http.StatusNoContent)

---

# 🔁 3xx — Redirect

## 301 Moved Permanently

## 302 Found

http.Redirect(w, r, "/new-path", http.StatusFound)

Чаще используется на уровне веба, не REST API.

---

# ❌ 4xx — Ошибка клиента

## 400 Bad Request

Неверный формат запроса.

http.Error(w, "Invalid JSON", http.StatusBadRequest)

---

## 401 Unauthorized

Нет авторизации или неверный токен.

http.Error(w, "Unauthorized", http.StatusUnauthorized)

Связано с:

- [[JWT]]
    
- [[Middleware]]
    

---

## 403 Forbidden

Клиент авторизован, но нет прав.

---

## 404 Not Found

Ресурс не существует.

http.Error(w, "Not Found", http.StatusNotFound)

---

## 405 Method Not Allowed

Метод не поддерживается.

---

# 💥 5xx — Ошибка сервера

## 500 Internal Server Error

Ошибка в коде сервера.

http.Error(w, "Internal Error", http.StatusInternalServerError)

---

## 502 Bad Gateway

Проблема при проксировании (часто в микросервисах).

---

## 503 Service Unavailable

Сервис недоступен (перегрузка, maintenance).

---

# 🧱 Правильное использование в REST

Пример CRUD:

|Операция|Код|
|---|---|
|GET /users|200|
|GET /users/1|200 или 404|
|POST /users|201|
|PUT /users/1|200 или 204|
|DELETE /users/1|204|

---

# 🧪 Полный пример в Go

func createUser(w http.ResponseWriter, r *http.Request) {  
	var user User  
  
	if err := json.NewDecoder(r.Body).Decode(&user); err != nil {  
		http.Error(w, "Invalid JSON", http.StatusBadRequest)  
		return  
	}  
  
	// допустим сохраняем  
  
	w.WriteHeader(http.StatusCreated)  
	json.NewEncoder(w).Encode(user)  
}

---

# ⚠ Важные моменты

## WriteHeader только один раз

w.WriteHeader(http.StatusOK)  
w.Write([]byte("OK"))

Если вызвать Write() раньше — статус будет 200 автоматически.

---

## Не возвращать 200 при ошибке

Плохая практика:

{  
  "status": "error"  
}

с кодом 200.

Клиент ориентируется на статус-код.

---

# 📦 Ошибки в JSON формате

Часто используют структуру:

{  
  "error": "invalid input"  
}

---

# 🔥 Статус-коды в микросервисах

Очень важны для:

- [[API Gateway]]
    
- [[Load Balancing]]
    
- [[Circuit Breaker]]
    

Если сервис отдаёт 500 → это сигнал проблемы.

---

# 🔗 Связанные темы

- [[HTTP]]
    
- [[REST API]]
    
- [[Middleware]]
    
- [[Router]]
    
- [[JSON]]
    
- [[JWT]]
    
- [[API Gateway]]
    
- [[Microservices]]
    

---

# 💎 Главное понимание

Статус-коды — это:

> Контракт между сервером и клиентом о результате запроса.

Они управляют логикой клиента:

- повторить?
    
- показать ошибку?
    
- авторизоваться?
    
- переключить маршрут?
    

---

# 🧠 Инсайт для тебя

Правильное использование статус-кодов — это признак зрелого backend.

Junior:

> всегда 200

Middle:

> понимает разницу между 400 и 422

Senior:

> думает о retry semantics и idempotency
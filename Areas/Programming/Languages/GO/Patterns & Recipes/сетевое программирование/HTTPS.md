# 🔐 HTTPS

## Что такое HTTPS

HTTPS = HTTP + TLS

То есть:

> HTTP-протокол, работающий поверх зашифрованного соединения.

Работает поверх:

- [[TCP]]
    
- использует [[TLS]]
    
- относится к прикладному уровню [[OSI Model]]
    

---

# 🧠 Зачем нужен HTTPS

Без HTTPS:

- данные передаются в открытом виде
    
- можно перехватить пароль
    
- можно изменить трафик (MITM атака)
    

HTTPS обеспечивает:

- 🔒 Шифрование
    
- 🧾 Проверку подлинности сервера
    
- 🛡 Целостность данных
    

---

# 🧱 Как работает HTTPS

## 1️⃣ Клиент подключается к серверу

Client → Server: "Хочу HTTPS"

---

## 2️⃣ TLS Handshake

Перед передачей HTTP происходит TLS-рукопожатие.

### Упрощённо:

1. Клиент отправляет список поддерживаемых алгоритмов
    
2. Сервер отправляет сертификат
    
3. Клиент проверяет сертификат
    
4. Стороны договариваются о ключах
    
5. Создаётся защищённое соединение
    

После этого начинается обычный HTTP.

---

# 📜 Сертификат

Сертификат содержит:

- публичный ключ сервера
    
- имя домена
    
- срок действия
    
- цифровую подпись центра сертификации
    

Пример:

example.com  
Issued by: Let's Encrypt

---

# 🏢 Центр сертификации (CA)

Это доверенная организация:

- Let's Encrypt
    
- DigiCert
    
- GlobalSign
    

Браузер доверяет сертификату, если он подписан известным CA.

---

# 🔐 Что происходит при запросе

Когда ты вводишь:

https://google.com

Происходит:

1. [[DNS]] возвращает IP
    
2. TCP соединение
    
3. TLS handshake
    
4. Проверка сертификата
    
5. Шифрование
    
6. HTTP запрос
    

---

# 🧪 HTTPS сервер в Go

## Самый простой вариант

http.ListenAndServeTLS(":443", "cert.pem", "key.pem", handler)

Где:

- cert.pem — сертификат
    
- key.pem — приватный ключ
    

---

# 🔧 Генерация самоподписанного сертификата

Для разработки:

openssl req -x509 -newkey rsa:4096 -keyout key.pem -out cert.pem -days 365 -nodes

Браузер будет ругаться, потому что это не доверенный CA.

---

# 🚀 HTTPS через Nginx (Production вариант)

Обычно Go-сервер не занимается TLS напрямую.

Архитектура:

Client  
   ↓  
Nginx (HTTPS)  
   ↓  
Go App (HTTP)

Nginx:

- принимает HTTPS
    
- расшифровывает
    
- проксирует в Go
    

Пример конфигурации:

server {  
    listen 443 ssl;  
    server_name example.com;  
  
    ssl_certificate /etc/ssl/cert.pem;  
    ssl_certificate_key /etc/ssl/key.pem;  
  
    location / {  
        proxy_pass http://localhost:8080;  
    }  
}

---

# 🖥 HTTPS через IIS

IIS (Internet Information Services) — веб-сервер от Microsoft.

Используется на Windows.

## Как включить HTTPS в IIS

1. Установить сертификат в Windows Certificate Store
    
2. Открыть IIS Manager
    
3. Выбрать сайт
    
4. Bindings → Add → HTTPS
    
5. Выбрать сертификат
    

IIS будет:

- принимать HTTPS
    
- расшифровывать
    
- передавать в приложение
    

---

# 🌐 HTTPS через Let's Encrypt

Бесплатные сертификаты.

Используется certbot:

sudo certbot --nginx -d example.com

Автоматически:

- получает сертификат
    
- настраивает nginx
    
- обновляет каждые 90 дней
    

---

# 🔁 HTTP → HTTPS redirect

http.ListenAndServe(":80", http.HandlerFunc(func(w http.ResponseWriter, r *http.Request) {  
	http.Redirect(w, r, "https://"+r.Host+r.RequestURI, http.StatusMovedPermanently)  
}))

---

# 🔐 mTLS (Mutual TLS)

Обычный HTTPS:

- клиент проверяет сервер
    

mTLS:

- сервер проверяет клиента
    

Используется в:

- [[Microservices]]
    
- внутренних системах
    

---

# 📦 HTTPS и HTTP/2

HTTP/2 почти всегда используется с TLS.

Поэтому:

- [[gRPC]] почти всегда работает через HTTPS
    

---

# ⚠ Что если сертификат просрочен

Браузер:

- блокирует соединение
    
- показывает предупреждение
    

API клиенты:

- отказываются подключаться
    

---

# 🔗 Связанные темы

- [[HTTP]]
    
- [[TCP]]
    
- [[TLS]]
    
- [[Certificates]]
    
- [[DNS]]
    
- [[Microservices]]
    
- [[Nginx]]
    
- [[IIS]]
    
- [[gRPC]]
    
- [[REST API]]
    

---

# 💎 Главное понимание

HTTPS — это:

> HTTP поверх зашифрованного канала, обеспечивающего конфиденциальность и подлинность.

Сам HTTP не меняется.  
Меняется транспорт.

---

# 🧠 Инсайт для тебя

Когда ты пишешь:

http.ListenAndServeTLS(...)

Под капотом:

- TCP соединение
    
- TLS handshake
    
- обмен ключами
    
- симметричное шифрование
    
- и только потом HTTP
# 📡 UDP (User Datagram Protocol)

## Что такое UDP

UDP — это транспортный протокол, работающий поверх [[IP]].

Главная особенность:

> Нет установления соединения и нет гарантий доставки.

Работает на том же уровне, что и [[TCP]], но гораздо проще.

---

# 🧠 Чем UDP отличается от TCP

|TCP|UDP|
|---|---|
|Есть соединение|Нет соединения|
|Гарантия доставки|Нет гарантии|
|Гарантия порядка|Нет гарантии|
|Повторная отправка|Нет|
|Медленнее|Быстрее|

---

# 📦 Как работает UDP

Когда ты отправляешь данные по UDP:

1. Данные помещаются в **датаграмму**
    
2. Добавляется UDP-заголовок (порт отправителя и получателя)
    
3. Пакет передаётся через [[IP]]
    
4. Всё
    

Никакого handshake.

---

# 🧾 UDP Header

UDP-заголовок содержит:

- Source Port
    
- Destination Port
    
- Length
    
- Checksum
    

Очень маленький — всего 8 байт.

Поэтому UDP быстрее.

---

# 🚀 Где используется UDP

- DNS
    
- Онлайн-игры
    
- VoIP
    
- Стриминг
    
- Real-time системы
    

---

# 🧪 UDP сервер в Go

## Пример UDP-сервера

package main  
  
import (  
	"fmt"  
	"net"  
)  
  
func main() {  
	addr, _ := net.ResolveUDPAddr("udp", ":8081")  
  
	conn, _ := net.ListenUDP("udp", addr)  
	defer conn.Close()  
  
	buffer := make([]byte, 1024)  
  
	fmt.Println("UDP server started on :8081")  
  
	for {  
		n, clientAddr, _ := conn.ReadFromUDP(buffer)  
  
		message := string(buffer[:n])  
		fmt.Println("Received:", message)  
  
		conn.WriteToUDP([]byte("PONG"), clientAddr)  
	}  
}

---

# 📤 UDP клиент в Go

package main  
  
import (  
	"fmt"  
	"net"  
)  
  
func main() {  
	addr, _ := net.ResolveUDPAddr("udp", "localhost:8081")  
  
	conn, _ := net.DialUDP("udp", nil, addr)  
	defer conn.Close()  
  
	conn.Write([]byte("PING"))  
  
	buffer := make([]byte, 1024)  
	n, _, _ := conn.ReadFromUDP(buffer)  
  
	fmt.Println("Server says:", string(buffer[:n]))  
}

---

# 🧠 Что важно понять

UDP:

- не гарантирует, что сообщение дойдёт
    
- не гарантирует порядок
    
- не повторяет отправку
    
- не управляет перегрузкой
    

Если нужно — ты сам должен это реализовать.

---

# 🔥 UDP — это fire-and-forget

Ты отправил:

conn.Write([]byte("hello"))

И всё.

Никакой проверки.

---

# 🧵 UDP и конкурентность

Если сервер получает много сообщений:

for {  
    go handlePacket(...)  
}

Можно обрабатывать каждую датаграмму в отдельной [[Areas/Programming/Languages/GO/Patterns & Recipes/Конкурентность/Goroutine]].

---

# ⚠ Ограничения UDP

- Максимальный размер датаграммы ~65KB
    
- Если пакет потерялся — никто не узнает
    
- Может прийти в неправильном порядке
    

---

# 🌐 UDP и NAT

UDP сложнее проходит через [[NAT]].

Для peer-to-peer используются:

- STUN
    
- TURN
    

---

# 📡 UDP vs HTTP

HTTP работает поверх [[TCP]].

Если тебе нужен:

- API
    
- REST
    
- надёжная передача
    

→ используешь TCP.

Если тебе нужен:

- real-time
    
- минимальная задержка
    

→ используешь UDP.

---

# 📌 Когда выбирать UDP

Используй UDP, если:

- допустима потеря данных
    
- важна скорость
    
- важна минимальная задержка
    

Не используй UDP для:

- API
    
- банковских операций
    
- файлов
    

---

# 🔗 Связанные темы

- [[TCP]]
    
- [[IP Address]]
    
- [[Socket]]
    
- [[Packet]]
    
- [[NAT]]
    
- [[OSI Model]]
    
- [[DNS]]
    

---

# 💎 Главное понимание

UDP — это:

> Минимальный транспортный протокол без гарантий.

Он быстрый, потому что почти ничего не делает.

TCP — умный и надёжный.  
UDP — простой и быстрый.
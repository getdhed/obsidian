
## Что такое JSON

JSON (JavaScript Object Notation) — это текстовый формат обмена данными.

Главная идея:

> Передавать структурированные данные в читаемом виде.

JSON чаще всего используется в:

- [[REST API]]
    
- [[HTTP]]
    
- конфигурационных файлах
    
- микросервисах
    

---

# 🧱 Структура JSON

JSON состоит из:

## 1️⃣ Объект (object)

Набор пар ключ–значение.

{  
  "name": "Nazar",  
  "age": 19  
}

---

## 2️⃣ Массив (array)

[  
  "Go",  
  "Python",  
  "Rust"  
]

---

## 3️⃣ Типы данных

JSON поддерживает:

- string
    
- number
    
- boolean
    
- null
    
- object
    
- array
    

---

# 🌐 Почему JSON так популярен

- Читаемый
    
- Лёгкий
    
- Кроссплатформенный
    
- Отлично работает с [[HTTP]]
    

---

# 🔄 JSON в REST

REST API почти всегда возвращает:

{  
  "id": 15,  
  "email": "test@mail.com"  
}

Передаётся в теле [[HTTP Response]].

---

# 🧠 Важно понимать

JSON — это просто текст.

Не структура в памяти.  
Не объект Go.  
Не struct.

Это строка байт.

---

# ⚙ Работа с JSON в Go

Go использует пакет:

import "encoding/json"

---

# 🧱 Структуры (Struct)

В Go JSON обычно мапится на struct.

type User struct {  
    ID    int    `json:"id"`  
    Name  string `json:"name"`  
    Email string `json:"email"`  
}

Вот это:

`json:"id"`

называется **struct tag**.

Он говорит Go, как называется поле в JSON.

---

# 📤 Marshal (Go → JSON)

Marshal — преобразование структуры в JSON.

user := User{ID: 1, Name: "Nazar", Email: "test@mail.com"}  
  
data, err := json.Marshal(user)

`data` — это `[]byte`

Результат:

{"id":1,"name":"Nazar","email":"test@mail.com"}

---

# 📥 Unmarshal (JSON → Go)

Преобразование JSON в структуру.

jsonData := []byte(`{"id":1,"name":"Nazar","email":"test@mail.com"}`)  
  
var user User  
err := json.Unmarshal(jsonData, &user)

Важно:

> Нужно передавать указатель (`&user`), потому что функция изменяет структуру.

---

# 🌐 JSON в HTTP handler

Пример обработки запроса:

func handler(w http.ResponseWriter, r *http.Request) {  
    var user User  
    json.NewDecoder(r.Body).Decode(&user)  
  
    w.Header().Set("Content-Type", "application/json")  
    json.NewEncoder(w).Encode(user)  
}

Связано с:

- [[HTTP Handler]]
    
- [[Context]]
    
- [[REST API]]
    

---

# 🔎 JSON и поля структуры

## Поле с маленькой буквы

Не экспортируется → не попадёт в JSON.

type User struct {  
    id int // не работает  
}

Go видит только экспортируемые поля (с большой буквы).

---

# 🧩 Опции struct tag

## omitempty

Email string `json:"email,omitempty"`

Если поле пустое — его не будет в JSON.

---

## Игнорирование поля

Password string `json:"-"`

Поле не попадёт в JSON вообще.

---

# ⚠ Частые ошибки

1. Забыл экспортировать поле
    
2. Передал не указатель в Unmarshal
    
3. Не проверил ошибку
    
4. Забыл закрыть r.Body
    

---

# 🚀 Advanced: map вместо struct

Можно декодировать в:

var data map[string]interface{}

Но:

- теряется типизация
    
- нужно делать type assertion
    

Используется реже.

---

# 🔗 Связанные темы

- [[REST API]]
    
- [[HTTP]]
    
- [[Struct]]
    
- [[Pointer]]
    
- [[Interface]]
    
- [[Middleware]]
    

---

# 📌 Главное понимание

JSON — это:

> Формат передачи данных.

В Go:

- Marshal → структура → JSON
    
- Unmarshal → JSON → структура
    

Он лежит в основе почти всех backend API.
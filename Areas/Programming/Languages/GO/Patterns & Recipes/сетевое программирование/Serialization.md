# 📦 Serialization (Сериализация)

## Что такое сериализация

Сериализация — это процесс преобразования структуры данных в последовательность байт для передачи или хранения.

Главная идея:

> Превратить структуру в формат, который можно передать по сети или записать в файл.

Обратный процесс называется:

> Десериализация (Deserialization)

---

# 🧠 Зачем нужна сериализация

Компьютер в памяти хранит:

- структуры
    
- указатели
    
- ссылки
    
- внутренние представления данных
    

Но по сети можно передавать только:

- байты
    

Поэтому нужно преобразование.

---

# 🔄 Пример

В памяти Go:

type User struct {  
    ID   int  
    Name string  
}

В сети (JSON):

{"id":1,"name":"Nazar"}

В сети (Protobuf):

бинарный поток байт

---

# 📡 Где используется сериализация

- [[REST API]]
    
- [[gRPC]]
    
- базы данных
    
- кэширование
    
- message brokers (Kafka, RabbitMQ)
    

---

# 🧱 Виды сериализации

## 1️⃣ Текстовая

- [[JSON]]
    
- XML
    
- YAML
    

Плюсы:

- читаемая
    
- удобна для отладки
    

Минусы:

- больше размер
    
- медленнее
    

---

## 2️⃣ Бинарная

- [[Protocol Buffers]]
    
- MessagePack
    
- Avro
    

Плюсы:

- компактная
    
- быстрая
    
- строгая схема
    

Минусы:

- нечитаемая
    
- требует генерации кода
    

---

# ⚙ Как это работает под капотом

Когда ты вызываешь:

json.Marshal(user)

Происходит:

1. Рефлексия по структуре
    
2. Чтение тегов `json:"..."`
    
3. Преобразование полей в строку
    
4. Запись байт
    

Когда ты вызываешь:

proto.Marshal(user)

Происходит:

1. Чтение схемы
    
2. Кодирование по номеру поля
    
3. Формирование бинарного формата
    

---

# 🧠 Важно понимать

Сериализация — это:

> Контракт между двумя системами.

Если формат изменится — взаимодействие сломается.

Поэтому важно:

- версионирование
    
- совместимость
    

---

# 🔥 Проблемы сериализации

## 1️⃣ Совместимость версий

Если убрать поле — старый клиент может сломаться.

Protobuf решает это через номера полей.

---

## 2️⃣ Безопасность

Нельзя доверять входящим данным.

Нужно:

- валидировать
    
- проверять типы
    
- обрабатывать ошибки
    

---

## 3️⃣ Производительность

JSON:

- больше CPU
    
- больше памяти
    

Protobuf:

- быстрее
    
- меньше размер
    

---

# 🌐 Связь с сетью

Когда ты вызываешь:

client.GetUser(...)

Происходит:

- сериализация запроса
    
- передача через [[TCP]]
    
- десериализация на сервере
    
- сериализация ответа
    
- обратная передача
    

---

# 🔗 Связанные темы

- [[JSON]]
    
- [[Protocol Buffers]]
    
- [[gRPC]]
    
- [[REST API]]
    
- [[HTTP]]
    
- [[TCP]]
    
- [[Schema]]
    

---

# 📌 Главное понимание

Сериализация — это:

> Преобразование структурированных данных в байты для передачи или хранения.

Без неё не существует ни одного сетевого протокола.

---

# 💎 Инсайт для тебя

Backend — это:

> Логика + сериализация + сеть.

Если убрать сериализацию — сервисы не смогут общаться.
# 🧩 API внутри приложения (Go): структура, слои, реализация

## Что такое API в рамках приложения

API — это слой “входа” в твою систему. Он:

- принимает запрос (HTTP/gRPC/…)
    
- валидирует и авторизует
    
- вызывает бизнес-логику
    
- возвращает ответ в понятном формате (JSON/Protobuf)
    
- логирует/метрит/трассирует
    

Важная мысль:

> API — это не бизнес-логика. API — это транспорт + адаптер.

Связанные темы: [[HTTP]], [[Router]], [[Middleware]], [[JSON]], [[gRPC]], [[Context]], [[Status Code]]

---

## Слои (правильная ментальная модель)

Один из самых рабочих вариантов:

1. **Transport / Delivery слой**  
    HTTP handlers (или gRPC handlers) + роутер + middleware.  
    Здесь НЕ должно быть логики “как считать цену” и т.п.
    
2. **Service / UseCase слой**  
    Бизнес-логика: операции, правила, проверки, orchestration.
    
3. **Repository / Storage слой**  
    Работа с БД/кэшем/файлами/внешними API. Никаких HTTP-деталей.
    
4. **Domain (опционально)**  
    Доменные типы, сущности, value objects.
    

Идея: API слой — тонкий. Основной мозг — в Service.

Связанные темы: [[Clean Architecture]], [[Dependency Injection]]

---

## Типовая структура проекта (пример)

Один из самых адекватных шаблонов:

cmd/  
  app/  
    main.go  
internal/  
  api/  
    http/  
      router.go  
      middleware/  
        logging.go  
        auth.go  
        recover.go  
      handler/  
        health.go  
        users.go  
      dto/  
        users.go  
      errors/  
        errors.go  
  service/  
    users.go  
  repo/  
    users_repo.go  
  config/  
  observability/  
pkg/ (если нужно вынести библиотечный код)

Где что:

- `handler/` — HTTP endpoints
    
- `dto/` — структуры “что приходит/уходит по API”
    
- `service/` — бизнес-методы
    
- `repo/` — БД
    
- `middleware/` — кросс-срезы: auth, logging, metrics
    

---

## Public API vs Private API (в одном приложении)

### Вариант A: разные префиксы/роутеры

- Публичное: `/api/v1/...`
    
- Приватное (админка/внутреннее): `/internal/...` или `/admin/...`
    

**Почему это удобно:** разные middleware, разные политики доступа, разные лимиты.

### Вариант B: разные сервера/порты

- public server: `:8080`
    
- internal server: `:9090` (доступен только внутри сети/VPN)
    

**Почему это круто:** ты физически отделяешь доступ.

---

## Роутинг: как строить API “как взрослые”

### net/http + ServeMux (минимально)

- годится для простого
    
- методы проверяешь вручную
    
- параметры пути парсишь сам
    

### chi / gin / echo (для нормального REST)

chi очень хорош для “тонкого фреймворка”.

Главная мысль:

> Роутер — это “карта” API. Он должен быть читаемым.

---

## DTO vs Domain model (очень важно)

### DTO (API structs)

То, что ты отдаёшь/принимаешь по сети.

### Domain model

То, как ты хранишь и используешь данные внутри бизнес-логики.

Причина разделения:

- API может меняться (поля/названия)
    
- доменная модель должна быть стабильной
    
- безопасность (не светить лишнее)
    
- контроль валидации и версий
    

Пример: `User` в домене может содержать `PasswordHash`, а в DTO — нет.

Связанные темы: [[Serialization]], [[JSON]]

---

## Handler: что он должен делать

Правильный handler:

1. достать `ctx := r.Context()`
    
2. распарсить вход (query/path/body)
    
3. провалидировать вход
    
4. авторизоваться (обычно через middleware)
    
5. вызвать `service.Method(ctx, ...)`
    
6. маппинг результата в DTO
    
7. отдать ответ + статус-код
    

**Он НЕ должен:**

- ходить в БД напрямую
    
- содержать бизнес-правила
    

---

## Базовый пример HTTP API (минимальный “скелет”)

### 1) Контракты (DTO)

type CreateUserRequest struct {  
    Name  string `json:"name"`  
    Email string `json:"email"`  
}  
  
type UserResponse struct {  
    ID    int64  `json:"id"`  
    Name  string `json:"name"`  
    Email string `json:"email"`  
}

### 2) Service интерфейс

type UserService interface {  
    Create(ctx context.Context, name, email string) (User, error)  
    Get(ctx context.Context, id int64) (User, error)  
}

### 3) Handler

type UserHandler struct {  
    svc UserService  
}  
  
func (h *UserHandler) Create(w http.ResponseWriter, r *http.Request) {  
    ctx := r.Context()  
  
    var req CreateUserRequest  
    if err := json.NewDecoder(r.Body).Decode(&req); err != nil {  
        writeError(w, http.StatusBadRequest, "invalid_json")  
        return  
    }  
    if req.Name == "" || req.Email == "" {  
        writeError(w, http.StatusBadRequest, "validation_error")  
        return  
    }  
  
    user, err := h.svc.Create(ctx, req.Name, req.Email)  
    if err != nil {  
        // тут маппишь ошибки сервиса → статус-коды  
        writeError(w, http.StatusInternalServerError, "internal_error")  
        return  
    }  
  
    w.Header().Set("Content-Type", "application/json")  
    w.WriteHeader(http.StatusCreated)  
    json.NewEncoder(w).Encode(UserResponse{ID: user.ID, Name: user.Name, Email: user.Email})  
}

### 4) Router

mux := http.NewServeMux()  
uh := &UserHandler{svc: userService}  
  
mux.HandleFunc("POST /api/v1/users", uh.Create) // Go 1.22+

> Если у тебя Go < 1.22 или хочешь удобнее — бери chi.

---

## Middleware: где живёт авторизация, логирование, метрики

### Типичные middleware

- logging
    
- request_id
    
- recover (ловить panic)
    
- auth (JWT/API key)
    
- rate limit
    
- metrics (Prometheus)
    
- tracing (OpenTelemetry)
    

Связанные темы: [[Middleware]], [[Prometheus]], [[Grafana]], [[OpenTelemetry]]

---

## Авторизация и аутентификация в API

### Частый вариант: Bearer token (JWT)

- Клиент кладёт `Authorization: Bearer <token>`
    
- Middleware валидирует токен
    
- Кладёт userID/role в [[Context]]
    
- Handler/Service читает данные из context
    

**Важно:**  
“JWT/токен” — это про **доступ к API**,  
а “таймауты соединения/idle” — это другое: это [[TCP]]/HTTP server timeouts и keep-alive.

---

## Context: как правильно использовать в API

- Всегда используй `r.Context()` в handler
    
- Передавай `ctx` дальше в service/repo
    
- Таймауты задавай там, где это уместно:
    
    - для внешних вызовов (HTTP client)
        
    - для запросов в БД
        
- `WithValue` — только для request-scoped метаданных (userID, requestID), не для зависимостей.
    

Связанные темы: [[Context]]

---

## Ошибки: единый формат + маппинг на статус-коды

Хороший API почти всегда имеет **единый формат ошибок**:

{  
  "error": "validation_error",  
  "message": "email is required"  
}

Правило:

- validation → 400
    
- not found → 404
    
- unauthorized → 401
    
- forbidden → 403
    
- conflict → 409
    
- internal → 500
    

Связанные темы: [[Status Code]]

---

## Версионирование API

Варианты:

- `/api/v1/...` (самый частый)
    
- заголовок `Accept: application/vnd...` (реже)
    
- поддомены (редко)
    

Совет:

> Начни с `/api/v1`. Это просто и практично.

---

## Таймауты и “соединение прервётся через 10 сек”

Это НЕ “токены соединения”.

Это настройки сервера/клиента:

- `ReadTimeout`, `WriteTimeout`, `IdleTimeout` у `http.Server`
    
- `Timeout` у `http.Client`
    
- reverse proxy (nginx/IIS) тоже может рвать соединение
    

То есть:

- токен (JWT) → авторизация
    
- таймаут/idle → транспорт и ресурсы сервера
    

---

## Наблюдаемость (что обычно “цепляют” к API)

В проде обычно обязательно:

- метрики → [[Prometheus]] + [[Grafana]]
    
- логирование → структурированные логи
    
- трассировка → [[OpenTelemetry]] + [[Jaeger]]
    

Как это цепляется:

- через middleware / interceptors
    

---

## Тестирование API внутри приложения

- unit-тесты сервисов без HTTP
    
- handler-тесты через `httptest.NewRecorder()` + `httptest.NewRequest()`
    
- интеграционные тесты: поднять сервер на случайном порту и прогнать реальные запросы
    

Связанные темы: [[Testing]], [[httptest]]

---

## Мини-чеклист “как писать API нормально”

-  API слой тонкий
    
-  единый формат ошибок
    
-  контекст прокидывается везде
    
-  авторизация в middleware
    
-  DTO отделены от домена
    
-  таймауты настроены
    
-  логирование/метрики/трейсинг есть
    
-  версия `/v1`
    
-  публичное и внутреннее API разделены (путь/порт)
    

---

## 🔗 Связанные темы (оставь как ссылки)

- [[HTTP]]
    
- [[HTTPS]]
    
- [[Router]]
    
- [[Middleware]]
    
- [[Context]]
    
- [[JSON]]
    
- [[Serialization]]
    
- [[Status Code]]
    
- [[JWT]]
    
- [[gRPC]]
    
- [[Protocol Buffers]]
    
- [[Prometheus]]
    
- [[Grafana]]
    
- [[OpenTelemetry]]
    
- [[Jaeger]]
    
- [[API Gateway]]
    
- [[Microservices]]
    
- [[Clean Architecture]]
    
- [[Dependency Injection]]
    
- [[Rate Limiting]]
    
- [[Logging]]